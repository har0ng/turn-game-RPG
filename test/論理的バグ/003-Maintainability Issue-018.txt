名称：データの変換

エラー(Type): C4244

発生行 (file:line): sfmlUI.cpp:1668

説明：=': 'T' から 'float' への変換です。データが失われる可能性があります。

エラーのシナリオ (修正前のコード): frameHeight = res.getTexture("fire").getSize().y;
        			
修正内容（簡単に): frameHeight = static_cast<float>(res.getTexture("fire").getSize().y);

エラー発見時期、発見者:11/18::17:21 , チャジファン

解決可否:O

